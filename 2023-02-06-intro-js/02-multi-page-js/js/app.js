let siteTitle = "Web Developer Fast Track";


/* Adding content to the page
   Ignore this code for now, we'll talk more about this in Days 6-10 */

// Store <div> object as a variable
const header = document.querySelector('.site-title div');

// Update the content of the <div>
header.innerHTML = siteTitle;

// Update the <title> of the page
document.title = siteTitle;